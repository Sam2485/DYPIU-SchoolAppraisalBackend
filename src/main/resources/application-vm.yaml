spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/director_appraisal}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

app:
  gcp:
    enabled: false
  upload:
    local-path: ${LOCAL_STORAGE_DIR:${UPLOAD_LOCAL_PATH:/opt/myapp/uploads}}
