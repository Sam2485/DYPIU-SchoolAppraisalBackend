spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

app:
  gcp:
    enabled: true
    bucket-name: ${GCP_BUCKET_NAME}
    project-id: ${GCP_PROJECT_ID}
